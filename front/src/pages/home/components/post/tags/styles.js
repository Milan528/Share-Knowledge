import { makeStyles } from '@material-ui/styles';

const classStyles = makeStyles({
   
    controlls: {
        display: "flex",
        alignItems: "center",
        flexWrap: "wrap",
        marginRight: -5,
    },

    tag: {
        marginRight: 5,
        marginBottom: 5,
    }


});

export default classStyles;

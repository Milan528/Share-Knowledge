import { makeStyles } from '@material-ui/styles';

const classStyles = makeStyles({
   
    photo: {
        // border: "solid 1px",
    },

    text: {
        display: "inline",
        marginLeft: 10,
    },

    container: {
        marginBottom: 10,
    }

});

export default classStyles;

import { makeStyles } from '@material-ui/styles';

const classStyles = makeStyles({
    button: {
        paddingLeft: 0,
        marginTop: 10,
        marginLeft: -5,
    },

    divider: {
        marginTop: 5,
        marginBottom: 20,
    }
});

export default classStyles;

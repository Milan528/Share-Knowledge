import { makeStyles } from '@material-ui/styles';

const classStyles = makeStyles({
   
    photo: {
        border: "solid 1px",
    },

    text: {
        marginBottom: 20,
        marginTop: 20,
    },

    divider: {
        marginTop: 5,
        marginBottom: 20,
    },

});

export default classStyles;

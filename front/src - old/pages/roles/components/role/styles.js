import { makeStyles } from '@material-ui/styles';

const classStyles = makeStyles({
    formControl: {
        minWidth: 120,
        width: 300,
        marginTop: 10,
    },
});

export default classStyles;

import { makeStyles } from '@material-ui/styles';

const classStyles = makeStyles({
    root: {
        color: "black",
        display: "grid",
    },

    divider: {
      marginBottom: 40,
    },

    width: {
      width: 30,
    }

});

export default classStyles;

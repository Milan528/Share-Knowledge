import { makeStyles } from '@material-ui/styles';

const classStyles = makeStyles({

    text: {
        marginBottom: 20,
        display: "flex",
        wordWrap: "break-word",
    },

    container: {
        display: "flex",
    },

   postType: {
    marginLeft: 20,
   },

});

export default classStyles;
